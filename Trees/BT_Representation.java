public class BT_Representation {

    // Node class to represent each node in the binary tree
    class Node {
        int data;
        Node left;
        Node right;

        // Constructor for creating a new node
        public Node(int data) {
            this.data = data;
            left = null;
            right = null;
        }
    }
}
